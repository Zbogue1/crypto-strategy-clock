# Data files managed by Railway — never overwrite with local versions
paper_portfolio.json
signal_credibility.json
position_state.json
signal_history.json
latest_analysis.json

# Logs
crypto_history.jsonl
crystal_ball_predictions.jsonl
crystal_ball_postmortems.jsonl

# Generated
crypto_dashboard.html
__pycache__/
*.pyc
stock_portfolio.json
fomo_aftermath.json
kalshi_event_portfolio.json
reports/

# Runtime learning output, rebuilt by the FOMO learning cycle. Its siblings
# (kalshi_event_portfolio.json, stock_portfolio.json) are already ignored;
# this one was just never added, so it shows up as untracked after every run.
fomo_lessons.json

# Scratch output from the /watch video pipeline — frames and downloaded video.
watch-out/
